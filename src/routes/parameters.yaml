parameters:
  data:
    name: data
    description: User data for creating.
    in: body
    required: true
    type: object
    properties:
      name:
        type: string
        example: ayo
      email:
        type: string
        example: yomionisade@gmail.com
      age:
        type: number
        example: 20
